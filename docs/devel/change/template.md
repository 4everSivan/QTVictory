# C[编号] - [变更简述]

> **卡片ID**: C[001~999] ｜ **类型**: [BugFix / Rollback / Refactor / Param] ｜ **状态**: [待核验 / 核验通过 / 核验驳回]
> **创建日期**: YYYY-MM-DD ｜ **关联设计**: [02-后端设计方案.md](../design/02-后端设计方案.md) §[章节]

---

## 1. 变更背景与动因

简要说明导致本次变更的直接原因：
- 发现的 Bug 表现、复现场景与根因定位；
- 或功能回调（Rollback）的具体背景与原设计的局限性；
- 避免空泛描述，说明“为什么非改不可”。

---

## 2. 规则与设计变更对照（人工核验重点）

| 维度 | 变更前（旧逻辑） | 变更后（新逻辑） | 取舍理由与影响边界 |
|---|---|---|---|
| **业务规则/阈值** | 例：单 tick 撮合量上限为 25% | 例：回调为 15% | 提高深度滑点仿真度，防止小盘股被击穿 |
| **错误处理/异常流** | 例：返回 500 内部错误 | 例：返回 400 校验错误并包含字段 | 提升前端错误透出能力 |
| **代码/接口契约** | 例：POST 请求缺少幂等校验 | 例：支持 Idempotency-Key 头 | 满足写操作安全性 |

---

## 3. 代码变更与受影响文件

- 核心实现：`backend/app/...` 或 `frontend/src/...`
- 测试覆盖：`tests/...`

---

## 4. 人工核验清单（Checklist）

> 核验人照表逐项执行，全勾确认后方可合入主干。

- [ ] **自动化测试**：执行针对性测试命令 `pytest tests/...` 或 `npm run test`，确认全绿。
- [ ] **行为/接口实测**：
  - 步骤 1：...
  - 步骤 2：...
  - 预期回执/现象：...
- [ ] **反向影响排查**：确认未破坏关联模块（如未破坏行情降级或持仓计算）。
- [ ] **设计方案与双向回链**：
  - [ ] 对应设计方案（[design/](../design/)）正文已更新自洽；
  - [ ] 对应设计方案章节原位已增补反向引用标签（`> 📌 **关联变更**: [C编号]...`）；
  - [ ] 对应设计方案头部“关联变更”追加本卡编号，文末变更记录追加回链条目；
  - [ ] [CHANGELOG.md](../../../CHANGELOG.md) 条目已登记并双向回链本卡。

---

## 5. 核验结论与收尾

- **核验方式**：AI 对话核验代签（由 AI 汇报测试事实与实测结果，人工在会话确认后代签收口）
- **确认人**：[人工姓名/用户名]（AI 代签）
- **核验日期**：YYYY-MM-DD
- **结论**：[核验通过 / 核验驳回]
- **会话确认记录**：[简述人机对齐确认的核心信息，如：人工在对话中确认日K数据已正常展示，同意合入]
- **版本归档**：
  - 关联提交：`[commit-hash]`
  - 纳入版本：`[vX.Y.Z](../../../CHANGELOG.md)`

# C003 - 交易员编辑态 strategyParams 读取端点

> **卡片ID**: C003 ｜ **类型**: Refactor ｜ **状态**: 待核验
> **创建日期**: 2026-09-17 ｜ **关联设计**: [02-后端设计方案.md §3.5](../design/02-后端设计方案.md#35-交易员服务traderservice)（接口契约见 §5.2 REST 表）

---

## 1. 变更背景与动因

- **现状缺口**：`PATCH /traders/{id}` 支持更新 `strategy_params`（设计 02 §5.2），但 `GET /traders/{id}` 详情只返回"账户+统计+持仓+最近成交+净值+活跃计划"，不含当前生效的 `strategyParams`——后端无任何单交易员策略参数读取路径。
- **为什么非改不可**（T19 决策③ 登记）：前端交易员编辑弹窗无法回填当前参数，只能以策略模板默认值为基底。用户此前自定义的参数在编辑态不可见，若以模板默认值基底提交 PATCH，将**静默覆盖**既有的自定义 `strategy_params`。
- **来源**：todo EN-0003（2026-09-17 登记，P2）；[M16 报告 §7](../report/02-M16-前端验收报告.md) 已知未做项同步登记。

---

## 2. 规则与设计变更对照（人工核验重点）

| 维度 | 变更前（旧逻辑） | 变更后（新逻辑） | 取舍理由与影响边界 |
|---|---|---|---|
| **业务规则/阈值** | 编辑以模板默认值为参数基底 | 编辑以当前生效参数为基底；模板默认值降级为显式"重置为模板"操作 | 消除静默覆盖风险；manual 交易员无策略参数语义不变 |
| **错误处理/异常流** | 无变化（404 等沿用 §5.1 错误模型） | 无变化 | — |
| **代码/接口契约** | 无 strategyParams 读取路径 | `GET /traders/{id}` 详情响应增补 `strategyParams` 字段（策略交易员含模板键与当前参数，manual 为 null） | 复用既有详情端点优于新增路由——前端编辑态本就要拉详情；响应体增量小，不新增鉴权面与端点维护面 |

---

## 3. 代码变更与受影响文件

- 核心实现：`backend/app/api/`（traders 详情端点响应组装）+ `backend/app/services/`（TraderService 详情视图）；前端编辑弹窗回填逻辑（T19 决策③ 对应组件）
- 测试覆盖：`backend/tests/service/test_trader.py` 增补详情含 `strategyParams` 断言；前端组件测试更新回填用例

---

## 4. 人工核验清单（Checklist）

> 核验人照表逐项执行，全勾确认后方可合入主干。

- [ ] **自动化测试**：`cd backend && .venv/bin/python -m pytest` 全绿（含新增 strategyParams 读取断言）；`cd frontend && npm run test` 全绿。
- [ ] **行为/接口实测**：
  - 步骤 1：创建策略交易员（引用模板 + 自定义参数），`GET /api/traders/{id}` 返回 `strategyParams` 与创建入参一致；
  - 步骤 2：`PATCH /api/traders/{id}` 修改 `strategy_params` 后再次 GET，返回更新后值；
  - 步骤 3：前端编辑弹窗打开即回填当前生效参数（非模板默认值）；
  - 预期回执/现象：manual 交易员该字段为 null，不报错。
- [ ] **反向影响排查**：详情端点既有消费方（交易员侧板/切换器/排行榜）不受响应体增量影响；导出与统计链路不波及。
- [ ] **设计方案与双向回链**：
  - [ ] 对应设计方案（[design/](../design/)）正文已更新自洽；
  - [ ] 对应设计方案章节原位已增补反向引用标签（`> 📌 **关联变更**: [C编号]...`）；
  - [ ] 对应设计方案头部"关联变更"追加本卡编号，文末变更记录追加回链条目；
  - [ ] [CHANGELOG.md](../../../CHANGELOG.md) 条目已登记并双向回链本卡。

---

## 5. 核验结论与收尾

- **核验方式**：AI 对话核验代签（由 AI 汇报测试事实与实测结果，人工在会话确认后代签收口）
- **确认人**：[人工姓名/用户名]（AI 代签）
- **核验日期**：YYYY-MM-DD
- **结论**：[核验通过 / 核验驳回]
- **会话确认记录**：[简述人机对齐确认的核心信息]
- **版本归档**：
  - 关联提交：`[commit-hash]`
  - 纳入版本：`[vX.Y.Z](../../../CHANGELOG.md)`
